{
  "name": "aarogyasathi-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency backend for AarogyaSathi (dev OTP auth + local JSON DB)",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test \"test/auth.test.js\"",
    "check": "node --check index.js && node --check src/config.js && node --check src/util.js && node --check src/db.js && node --check src/auth.js && node --check src/router.js && node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}